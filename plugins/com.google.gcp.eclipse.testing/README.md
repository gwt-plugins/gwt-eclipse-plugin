#com.google.gcp.eclipse.testing

##Requirements
Either set a system environmental variable for GAE_HOME to the SDK or Maven will provide it through the pom.xml. 
Put the SDK in the ./resources directory.
