<project name="com.gwtplugins.gdt.eclipse.suite.update" default="build" basedir=".">

  <!-- Project-specific properties -->
  <property name="plugin_name" value="com.gwtplugins.gdt.eclipse.suite.update" />
  <property name="project.tail" value="plugins/${plugin_name}" />

  <property name="plugin.version" value="${gdt.version}" />

  

  <target name="build">
    <copy.plugin.src />

  </target>
</project>
